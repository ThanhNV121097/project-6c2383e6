# Local PostgreSQL user.
POSTGRES_USER=app
# Local PostgreSQL password. Replace outside local development.
POSTGRES_PASSWORD=app_secret
# Local PostgreSQL database name.
POSTGRES_DB=app
# Browser-visible backend API URL.
NEXT_PUBLIC_API_URL=http://localhost:8080
# Optional host ports.
BACKEND_PORT=8080
FRONTEND_PORT=3000
