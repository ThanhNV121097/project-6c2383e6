# Runtime PostgreSQL connection string.
DATABASE_URL=postgres://app:app_secret@localhost:5432/app?sslmode=disable
# HTTP listen port. Runtime supplies this key.
PORT=8080
# Optional fallback used only when PORT is unset.
APP_PORT=8080
